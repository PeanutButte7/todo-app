import {styled} from '@stitches/react';

export const TaskDashboardStyled = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '60px',
});

export const TaskGroupStyled = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '20px',
});